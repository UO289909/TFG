import { View, Text } from 'react-native';
import { globalStyles } from '../../../config/app-theme';

export const HomeScreen = () => {
  return (
    <View>
        <Text style={ globalStyles.titleText }>Inicio</Text>
    </View>
  );
};
